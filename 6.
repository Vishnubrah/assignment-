#include <stdio.h>

float square(float num) {
    return num * num;
}

int main() {
    float num, result;
    printf("Input any number for square:");
    scanf("%f", &num);
    result = square(num);
    printf("The square of %0.2f is :%0.2f", num, result);
    return 0;
}
